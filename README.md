Very WIP.

Im planning a huge rewrite(almost from scratch, to optimise and organise this project better)

Current goal: full jvm bytecode implementation(tableswitch, lookupswitch, wide, other DUPs than DUP) -> CLDC + freeRTOS + Windows support(threading) -> MIDP library
